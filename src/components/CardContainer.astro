---
type Props = {
  class?: string;
};

const { class: className = "" } = Astro.props;
---

<div class:list={["grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 mt-6 gap-x-4 gap-y-7", className]}>
  <slot />
</div>
